import {getEvidence,bindings} from '@/lib/later/server';
export async function GET(req:Request,ctx:{params:Promise<{id:string}>}){const {id}=await ctx.params;const e=await getEvidence(id);if(!e)return new Response('Evidence not found',{status:404});const file=await bindings().BUCKET.get(e.key);if(!file)return new Response('The original file is unavailable',{status:404});return new Response(file.body,{headers:{'Content-Type':e.mime,'Content-Disposition':`inline; filename*=UTF-8''${encodeURIComponent(e.filename)}`,'X-Content-Type-Options':'nosniff','Cache-Control':'private, no-store','Content-Security-Policy':"default-src 'none'; style-src 'unsafe-inline'; sandbox"}});}
